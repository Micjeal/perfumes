{
  "name": "perfumes-shop",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "netlify deploy --prod --dir=dist"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "dependencies": {
    "path-browserify": "^1.0.1"
  }
}
